# P2B 持久工作记录

状态：**static_v2 记录器回归已通过，T-DT 静态首例失败；Nav2 代价语义修复待 Terra 验证。**
P2A 基线 `ca608e5`；初次 P2B `b1c40e6`；记录器修复 `e85b076`。
本轮开发基线 `e85b076`，分支 `experiment/tdt-planner-phase2`；交付提交执行前核对。

## 已有证据

- static_v1：共享 rosout 等级错误中断，原始目录/报告完整保留。
- static_v2：e85b076 构建成功、39/39 通过；Navfn、Smac2D 各 5/5 有限静态通过。
- T-DT A* 与 QP 首例均有有效失败证据，各恢复 16 次，按门停止 2–5；P2B 未通过。
- 两组各一次 snapshot 更新拒收；另有 15/14 次端点保守自由空间拒收，不混计。
- LIO teardown 单次 -11 保留为独立问题，尚未归因规划器或更改定位代码。

## 本轮实现与待验

- 明确区分默认离线占用种子和 Nav2 master 输入，修正 253 内切区重复膨胀；
  253 中心禁区、254/255/边界的完整车体半径和安全余量、更新/迟到拒收保持。
- 新增端点与地图诊断、5 项核心和 1 项插件回归；常规预期 45 项，sanitizer 预期
  24 项核心。已提供持久 `sanitizers` 执行入口，**尚未编译或运行**。
- 对旧地图/事件的只读数值分析已保存，不能当作修复后的规划或仿真通过。
- 166 个 static_v2 原证据文件/回传报告已核对哈希，现场配置与旧证据不修改。

## 下一执行者

源码及全部构建/日志保存在 `/home/wpie/worktrees/rm2027_tdt_phase2`，运行目录
为 `build/tdt_p2b/`，内部临时文件为其 `tmp/`。保留 static_v1、static_v2，下一轮
使用新 static_v3。中断先核对已有记录，不覆盖编号或删除 build。

Terra 按 [代价语义修复交接](p2b_costmap_semantics_handoff.md) 执行，用
[结果模板](p2b_result_template.md) 写新报告。依次构建、45 项测试、24 项 sanitizer、
四 profile 核对，逐组首例；人工审查通过的组才补 2–5。共用故障停全部，功能或安全
失败停对应组重复。修复仍需实测，当前不提高此前约 55% 的规划迁移进度估算。

设计者本轮只做代码/数据审阅、语法与哈希核对；移动障碍、全负载及实车未执行。
当前设备性能限制只记录，等待新设备验证，不做性能补丁或淘汰候选。
