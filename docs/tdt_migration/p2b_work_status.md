# P2B 持久工作记录

状态：**工具实现完成，等待 Terra 验证**。这是开发状态，不是验收报告。
开发基线：`ca608e5`；分支：`experiment/tdt-planner-phase2`。
交接提交：使用本分支随用户交付的提交号，执行前核对 `git rev-parse HEAD`。

## 持久目录

- 源码：`/home/wpie/worktrees/rm2027_tdt_phase2`。
- 求解器依赖/构建/配置/运行：该目录的 `build/tdt_p2b/`（现有 git ignore）。
- 后续内部临时目录：`build/tdt_p2b/tmp/`，不依赖主机 `/tmp`。
- 正式运行系列：`build/tdt_p2b/runs/static_v1/`，验证时才创建。

## 已完成

- 前一轮已把固定依赖与构建缓存从 `/tmp` 复制到持久目录。
- 前一轮已有 6 个仿真包的构建记录；不等于本轮新增工具通过验证。
- 本轮已保存静态仿真入口、四配置生成/核对、ROS 记录器、单次容器运行/清理脚本、
  原始证据复算和矩阵汇总、13 个工具测试用例。
- 已提供详细验证步骤、通过/失败口径、设备约束和结果回传模板。
- 本轮不修改 core/vendor 算法、实车配置或既有仿真 MPPI/costmap 参数。

## 下一执行者

Terra 按 [交接文档](p2b_terra_handoff.md) 执行；按
[回传模板](p2b_result_template.md) 写实际结果。新 13 例测试和新工具的构建、ROS/Gazebo
运行均待验证，既有 22 例回归也由 Terra 执行。设计者本轮只做源码审阅和静态语法核对。
如会话中断，先核对已有运行目录和报告再继续，不覆盖任何编号。

功能/安全或记录器问题带最小证据交回设计者；当前设备性能限制只记录，不做性能
补丁或淘汰候选。移动障碍、全负载和实车不在本次交接执行范围。
